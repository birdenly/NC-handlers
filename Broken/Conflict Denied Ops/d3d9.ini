[MAIN]
;If resolution is lower than 640x480, screen resolution is used
ResolutionX = 800
ResolutionY = 600
ForceWindowedMode = 1
FOV = 80
;If 0, FPS limit is disabled
FPSLimit = 0
;Camera sensitivity when not using iron-sights. Default 0.5, ~10.0 seems to be more or less equal to a one when looking down sights
FreeLookSensitivty = 10