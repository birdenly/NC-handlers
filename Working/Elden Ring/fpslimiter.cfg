InitialFps=30.0 // Set this value to the desired initial maximum framerate (equal or greater than 1).
ToggleFps=30.0 // Set this value to the desired toggle framerate (large number disables limit).
CompatibleMode=0 // Set to 1 to disable optimizations that are incompatible with some monitoring apps (may introduce stutter).
